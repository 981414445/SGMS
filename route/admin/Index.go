package admin

import "github.com/kataras/iris"

const (
	ADMIN_VERSION = "1.0.0"
	ADMIN_TITLE   = "学生成绩管理系统 | 管理后台"
)

func Init(app iris.MuxAPI) {
	// routeAdmin(app)
	// routeUser(app)
	// routeTraining(app)
	// routeSelect(app)
	// routeSMS(app)
	// routeCourse(app)
	// routePaper(app)
	// routeQuiz(app)
	// routeActivityUser(app)
	// routeOrder(app)
	// routeCommon(app)
	// routeStatistics(app)
	// routeResource(app)
	// routePublic(app)
	// routeTeacher(app)
	// routeVideo(app)
	// routeGame(app)
	// routeGameRule(app)
	// routeActivitySystem(app)
	// routeVideoPlaylist(app)
	// routeRecommendContent(app)
	// routeCouponUser(app)
	// routeAchievement(app)
	// routeHistory(app)
	// routeUserEvaluation(app)
}
