package table

import (
	"testing"
)

func TestTables(t *testing.T) {
	// mysql := db.InitMysql()
	// defer mysql.Db.Close()
	// a := ActivityFetch(mysql, 26)
	// fmt.Println(a)
	// ActivityAdd(mysql, a)
	// fmt.Println(a)
	// a.Name += "test"
	// ActivityUpdate(mysql, *a)
	// ActivityDel(mysql, a.Id)
}
